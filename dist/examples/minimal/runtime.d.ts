/**
 * THE SMALLEST COMPLETE ADAPTER — copy this file, change the strings, ship.
 *
 * A neighbourhood bakery: no AI voice agent, no partner site to cross-promote, no professional
 * licence, no service areas. It exists to prove the engine carries no industry assumptions — the
 * only required identity is a name, a URL, a host, and an agent name.
 *
 * Run it with:
 *   npx tsx examples/minimal/generate.ts --count=1 --dry-run
 */
import type { BlogEngineRuntime } from '../../src/index.js';
export declare function minimalBlogRuntime(): BlogEngineRuntime;
//# sourceMappingURL=runtime.d.ts.map